VLLM_MODEL=grader
VLLM_MAX_TOKENS=4096
VLLM_TIMEOUT=600
VLLM_BASE_URL="
http://localhost:8001/v1,http://localhost:8002/v1,http://localhost:8003/v1,http://localhost:8004/v1,
"

# Optional throttling and concurrency controls (useful for remote OpenAI-compatible endpoints)
# MIN_INTERVAL_S=0.11        # fixed per-URL min interval (~545 RPM per URL)
# MIN_INTERVAL_S=-0.1        # jittered interval per request (0–0.1s), mainly to smooth bursts
# GRADER_MAX_WORKERS=64      # total grading threads per process
